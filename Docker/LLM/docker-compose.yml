name: LLM_Ollama

services:
  ollama:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: LLM_Ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
